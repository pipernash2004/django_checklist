from django.urls import path, include
# from rest_framework.routers import DefaultRouter

# from .views import (
#     ChecklistTypeViewSet,
#     ChecklistViewSet,
#     ListItemViewSet,
#     ChecklistItemViewSet,
# )

# router = DefaultRouter()

# router.register(r'checklist-types', ChecklistTypeViewSet, basename='checklisttype')
# router.register(r'checklists', ChecklistViewSet, basename='checklist')
# router.register(r'list-items', ListItemViewSet, basename='listitem')
# router.register(r'checklist-items', ChecklistItemViewSet, basename='checklistitem')
# # router.register(r'crew-member-checklists', CrewMemberChecklistViewSet, basename='crewmemberchecklist')

# urlpatterns = [
#     path('', include(router.urls)),
# ]